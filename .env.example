# Copy to .env and replace values before deploying
SECRET_KEY=replace_with_a_random_secret
# Use your PostgreSQL URL for production on Koyeb (example placeholder)
DATABASE_URL=postgresql://user:password@host:5432/worksmart
# If you want to test locally without Postgres, delete DATABASE_URL from env and SQLite will be used.
# To auto-seed sample data on first run, set SEED_DATA=1
SEED_DATA=1
